build/
profile/
repomix.config.json
repomix-output.xml

# uv / python
.venv/
__pycache__/
*.pyc
.python-version

# AppImage build outputs / tooling
*.AppImage
appimagetool
build/AppDir/

# Flatpak build outputs (single-file bundle + builder state)
*.flatpak
build/flatpak-stage/
build/flatpak-build/
build/flatpak-repo/
.flatpak-builder/

# Windows portable build outputs
build/win-portable/

# Windows installer build outputs (generated by CI from branding/aph.png)
packaging/aph.ico
packaging/Output/

# Manually downloaded vendor assets (fetch.py uses temp dirs; never commit these)
Firefox Setup *.exe
*.msi

# OS droppings (Windows Explorer / Finder touch every browsed folder)
Thumbs.db
desktop.ini
.DS_Store

# Editors / IDE
/.vscode/
/.idea/
*~
*.swp

# Node (no deps today; keeps one stray `npm install` out of history)
node_modules/

# Logs
*.log
